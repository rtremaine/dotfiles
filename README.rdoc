== Prereqs
  install bash completion
    macports: sudo port install bash-completion
    homebrew: brew install bash-completion
    apt-get: sudo apt-get install bash-completion

== Installation

  git clone git://github.com/rtremaine/dotfiles ~/.dotfiles
  cd ~/.dotfiles
  rake install
